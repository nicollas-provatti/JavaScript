// A tipagem fraca do JavaScript significa que a linguagem permite que variáveis mudem de tipo dinamicamente e converte automaticamente valores entre diferentes tipos quando necessário. Isso pode trazer flexibilidade, mas também pode causar comportamentos inesperados.

let valor = "Carlos";
console.log(valor);

valor = 18;
console.log(valor);

valor = 1.56;
console.log(valor);