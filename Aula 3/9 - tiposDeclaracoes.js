console.log(soma(2, 3))
console.log(sub(2, 3)) // erro
console.log(mult(2, 3)) // erro

// function declaration
function soma(x, y) {
    return x + y
}

// function expression
const sub = function (a, b) {
    return a - y
}

// named function expression
const mult = function mult(a, b) { // ajuda na hora de debugar o código
    return x * y
}