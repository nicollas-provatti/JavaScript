function somar(n1, n2) { // Declaração
    return n1 + n2;
}

let soma = somar(4, 3); // Chamada
console.log(soma);

let n1 = 5, n2 = 2;
soma = somar(n1, n2); // Chamada

console.log(soma);

// Parâmetros x Argumento e Retorno.